[GitHub](https://github.com/asheroto/Root-Certificate-Updater)

A script to update root certificates on [[Windows]]. This fixes the `self-signed certificate in certificate chain` error.

I [[Use|use]] it.